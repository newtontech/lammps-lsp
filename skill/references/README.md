# LAMMPS LSP Skill References

This directory is intentionally lightweight. The packaged skill points agents to
the installed `lammps-lsp-tool` CLI and the official LAMMPS manual rather than
bundling large documentation snapshots.

Use `lammps-lsp-tool check <input-file> --format json --fail-on-blocking` as the
readiness gate for generated input scripts.
